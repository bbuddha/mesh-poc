type: MeshGateway
mesh: default
name: mesh-gateway
selectors:
  - match:
      kuma.io/service: mesh-gateway
conf:
  listeners:
    - port: 8080
      protocol: HTTP
      tags:
        port: http/8080
